# PlotHole Web

Production frontend for PlotHole built with Next.js 14 App Router, Tailwind CSS, and Supabase.

## Setup

1. Copy `.env.example` to `.env.local`
2. Install dependencies: `npm install`
3. Run dev server: `npm run dev`
